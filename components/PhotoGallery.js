import Link from 'next/link';
import Image from 'next/image'

const PhotoGallery = () => {
    const photoContent = (
        <div>
            <h3>TEST Photo Gallery</h3>
            <p>Here&apos;s the gallary</p>
        </div>
    )

    return photoContent;
};

export default PhotoGallery;